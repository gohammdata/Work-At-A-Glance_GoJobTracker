{{ define "Show" }}
    {{ template "Header" }}
        {{ template "Menu"}}
        <h2> Registered {{ .Id }} </h2>
            <p>Name: {{ .Name }} </p>
            <p>City: {{ .City }}</p><br />
            <a href="/edit?id={{ .Id }}">Edit</a>
            </p>
    {{ template "Footer" }}
{{ end }}
